//d. Algoritmo 4
const arr = [1,2,3,4,5]// Variável declarada em const e contendo uma array de números inteiros 
const output = arr.filter((x)=>  x > 4);// Variável declarada em const e contendo uma lista selecionada pelo método filter
console.log(output);// método para exibir a lista contida na variável output

//Semântica: Algoritmo que retorna uma lista selecionada com valores abaixo de 4
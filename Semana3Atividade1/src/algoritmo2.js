//a. Algoritmo 2
const numbers = [1,2,3,4,5];// Variável declarada em const e contendo uma array de números inteiros 
const output = arr.map((x)=>{return x * 2;});// Variável declarada em const e contendo uma lista com os elementos de numbers modificados
console.log(output);// método para exibir a lista contida na variável output

//Semántica: Algoritmo que retorna uma lista com os elementos de numbers dobrados